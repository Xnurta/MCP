# Basic Entity List Query (Profile List)

```json
{
  "profileIds": [4404871489220462],
  "entity": "profile",
  "userContext": "View the user's list of stores"
}
```

Note: no `dateStart`/`dateEnd`/`metrics` — this tool never takes them. `entity` is required on every call.
